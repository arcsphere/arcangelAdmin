import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Allow the admin panel to run on /admin-v3 base path
  // Remove if running as a standalone app on its own domain
};

export default nextConfig;
